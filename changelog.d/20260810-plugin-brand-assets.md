## Added

- Package the approved standard, dark-mode, and compact VivagoAgent Logo artwork as editable SVG and sRGB PNG assets.
- Configure the Codex plugin to use root-level `assets/` artwork and the primary brand color `#574DFF`.
