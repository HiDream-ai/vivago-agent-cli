# Added

- Added the initial VivagoAgent local CLI architecture, existing-login adapter, REST/SSE client contract, and cross-host plugin bundle.
- Added unit tests and plugin validation commands.

Compatibility: this is the initial `0.1.0` contract.

Verification: run the commands documented in `AGENTS.md`.
