# Added

- Add Go `project assets`, `cancel`, and `history` JSON commands using the existing Web API contracts.
- Validate pagination, optional asset cursors, conversation IDs, and turn IDs before making network requests.
