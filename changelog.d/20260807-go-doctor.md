## Added

- Add a Go `doctor` command that reports build metadata, supported OS/CPU, fixed overseas environment target, credential backend, Linux file fallback, and login state.

## Security

- Keep doctor output free of account identifiers, tickets, refresh tokens, cookies, authorization headers, and credential-store contents.
- Let doctor report an unavailable credential backend as a failed check instead of crashing during runtime initialization.
