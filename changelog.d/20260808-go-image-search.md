## Added

- Add an explicit `ask --image-search` option for user-authorized online image and visual-reference search on a new VivagoAgent Turn.
- Teach the bundled Codex and Claude Code skill when to enable image search and clarify that `resume` continues the existing Turn without resubmitting the option.
