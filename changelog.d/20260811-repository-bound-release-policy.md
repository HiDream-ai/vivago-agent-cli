# Release safety

- Add fail-closed release policy checks so the personal GitHub repository can publish only
  development versions and the company GitHub repository can publish Beta versions only from
  `main` with a full source revision.
