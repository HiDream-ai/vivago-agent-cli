# 标准环境代理兼容

附件上传和产物下载现在与普通 API 一样遵循 `HTTP_PROXY`、`HTTPS_PROXY` 和 `NO_PROXY`。未配置
代理时继续使用安全直连；配置本地或企业代理时使用代理，同时保留业务目标 URL、公网 DNS、重定向、
文件类型和大小检查。下载网络错误不再回传可能包含代理凭证的底层错误文本。

通过本地代理拨号、无代理直连、私网目标拒绝、代理凭证脱敏和上传/下载完整包测试验证。
