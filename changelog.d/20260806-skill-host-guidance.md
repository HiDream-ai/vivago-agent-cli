# Documentation

- Restructured the skill into progressive-disclosure layers: SKILL.md now carries only the mandatory host behaviors organized by service moment (gate, preconditions, before submitting, start, while it runs, deliver, continue/recover/cancel, errors), with five reference files shipped alongside for on-demand depth (brief guide, recovery runbook, output shapes, delivery playbook, capability limits). Folds in the twelve UX-review findings: background execution for long Turns, milestone relay and status answers, batched pre-submission clarification with a recap, a working-directory task ledger, history-based re-entry, replying in the user's language, delivery hierarchy with proactive next steps, mid-stream question surfacing, cancel's asynchronous semantics, conflict recovery paths, an image fast path, and degraded media presentation.

- Expanded the plugin SKILL.md host guidance from pilot field usage: video brief structure with per-second beats, task expectation setting, project deep-link sharing, delivery verification against the brief, known capability limits with host-side fallbacks, an explicit resume-loop contract for long streams, conversation-level iteration with asset reuse, and user-facing phrasing per exit code.
- Hardened SKILL.md against common host failure modes surfaced by competitor teardown (Dreamina CLI, ChatCut, Higgsfield skills): host environment gate before any work, mandatory post-login outcome reporting, quota-aware submission guidance with a per-task identifier ledger, canonical JSON output shapes for `ask` / `stream_error` / `artifact preview`, an explicit pilot-environment note for the pinned `--env overseas-dev`, and image/video trigger phrasing in the skill description.
- Corrected the video brief input convention: storyboards must use numbered scenes (Scene 1 / 场景 1), not a time-stamped global timeline — the storyboard stage does not parse time ranges. Total duration is stated once as a hard constraint in the overall prompt; scene entries carry scene content only, with no per-scene timing, and the pipeline allocates time across scenes.
